# PR validation: build the current upstream stable + run the smoke suite.
# Publishes nothing.
name: pull-request

on:
  pull_request:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'scripts/**'
      - '.github/workflows/**'
      - 'compose.yaml'
      - 'docker-compose.yaml'

jobs:
  test-build:
    name: Build & smoke (no publish)
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    with:
      version: '2.59.0'
      commit: '134c92a01b120162f00c7275189cc47858720379'
      publish: false
    secrets: inherit
